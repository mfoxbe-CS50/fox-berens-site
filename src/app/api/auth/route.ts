import { NextResponse } from 'next/server'

export async function GET() {
  // Placeholder — auth callback handler will be implemented with Supabase Auth
  return NextResponse.json({ status: 'ok' })
}
